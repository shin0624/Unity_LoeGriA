using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Poolable : MonoBehaviour
{
    //Pooling할 객체가 들고 있는 컴포넌트. Pooling하지 않을 객체는 들고있지 않는다.
    public bool IsUsing;
}
